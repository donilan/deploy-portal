%table.table
  %thead
    %tr
      %th Key
      %th Value
      %th &nbsp;
  %tbody
    - @settings.each do |s|
      %tr
        %td= s[0]
        %td= s[1]
        %td= link_to 'Edit', edit_setting_path(s[0])
